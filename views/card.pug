extends layout.pug
block content
  section#content
    h2 #{text}
    if hint
      p
        i Hint: #{hint}
    else
      p This question is easy, therefore there is not hint.
    a(href=`${id}?side=${sideToShow}`)= sideToShowDisplay
